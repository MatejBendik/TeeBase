/* const loginReducers = (state: any, action: any) => {
  switch (action.type) {
    case "NEW_USER":
      return [...state, action.payload];

    default:
      return state;
  }
};
export default loginReducers; */

export {};
